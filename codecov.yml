coverage:
  status:
    project:
      default: off
      protocol:
        target: 95%
        flags: protocol

flags:
  protocol:
    paths:
      - packages/protocol/

fixes:
  - "packages/protocol/::"
