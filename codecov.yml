coverage:
  status:
    project:
      default: off
      protocol:
        target: 95%
        flags: protocol

flags:
  protocol:
    paths:
      - packages/protocol/

fixes:
  - "packages/protocol/::" # move path   e.g., "before/path" => "after/path"
